# Testbench

Testbench(이하 `TB`) 는 테스트 중인 장치(Device Under Test, `DUT`), 즉 설계한 RTL 코드가 올바르게 동작하는지 확인하기 위해 만드는 환경이다.

입력과 그에 따라 출력될 것으로 고려되는 패턴을 `Test Vector` 라고 하며, 이것이 DUT 의 실제 입출력과 동일하도록 검증해야 한다. 이를 통해 설계자가 예상하지 못한 논리적 버그를 디버깅할 수 있다.

## TB codes
 · [ALU]()
